holidays==0.48
matplotlib==3.10.1
numpy==1.24.3          # Must be >=1.24 to provide np.bool8 for Plotly
pandas>=2.0            # >=2.0 is compatible with Py 3.12
pillow==11.1.0         # Lowercase "pillow" is standard, same version
plotly==5.13.1         # Modern Plotly that uses np.bool8
prophet==1.1.6
scikit-learn>=1.3
statsmodels==0.14.4
streamlit==1.41.1
tsdb==0.6.2
xgboost==3.0.0
# Ensure modern setuptools/wheel so builds on Py 3.12 don’t break
setuptools>=65.5
wheel>=0.38
