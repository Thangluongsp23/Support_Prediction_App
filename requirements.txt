holidays==0.48
matplotlib==3.10.1

# NumPy 1.24.x has np.bool8, but we plan to avoid
# the bool8 call by downgrading Plotly. Still good to keep a modern NumPy:
numpy==1.24.3

# Pandas 2.x is recommended for Python 3.12
pandas==2.0.3

# Keep only one Pillow line
pillow==11.1.0

# Downgrade Plotly so it doesn't reference np.bool8 in imshow_utils
# Versions like 5.6.0 or 5.5.0 are known not to call np.bool8
plotly==5.6.0

prophet==1.1.6

# scikit-learn 1.3.0 or higher is better for Python 3.12
scikit-learn==1.3.0

statsmodels==0.14.4
streamlit==1.41.1
tsdb==0.6.2
xgboost==3.0.0

# Streamlit 1.41.1 wants rich<14, so pick a version under 14
rich==13.4.2

# Modern setuptools/wheel can help avoid distutils issues
setuptools>=65.5
wheel>=0.38

wheel>=0.38

